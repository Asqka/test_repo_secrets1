name: Test secrets 

on:
  push:
  pull_requests:
  worflow_dispatch:

  jobs:
    test:
      runs-on: ubuntu-latest

      steps:
        - name: Test simple run
           run: echo "Test simple run"
